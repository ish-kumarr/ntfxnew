'use client';

// The /downloads and /platforms paths share the same exact component and layout.
import PlatformsPage from '@/app/platforms/page';

export default function DownloadsPage() {
    return <PlatformsPage />;
}
